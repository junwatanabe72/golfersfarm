<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
  <img src="main_visual.jpg" >
  <div>
<% else %>

<div class="bimage">
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Welcome to the Golfersfarm</h1>
       <%= link_to 'Sign up now!', signup_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>
